import '/resource/no.hib.masters.model/model/model.ecore'

package model

context Label

inv SuffientConstrast('The label "' + self.toString() + '" has an insufficient constrast ratio of 1:' + 
	self.constrastRatio.toString() + ' to its background'):
	 constrastRatio >= 7

endpackage
