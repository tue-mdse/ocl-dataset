--translate a -1 value to the string "w[" or to the normal "maxtime]" closure
helper
def: translatetoinf(maxtime: Integer): String = 
     if (maxtime = -1)
     then
         'w['
     else
          maxtime.toString() + ']'
     endif;
