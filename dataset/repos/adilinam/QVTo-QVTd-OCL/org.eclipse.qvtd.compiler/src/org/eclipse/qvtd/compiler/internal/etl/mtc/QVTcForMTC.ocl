import 'platform:/resource/org.eclipse.qvtd.pivot.qvtcorebase/model/QVTcoreBase.ecore'

package qvtcorebase

context PropertyAssignment
--
-- All variable references in the value attribute must have types in the same domain
--
inv singleDomainValue(self.toString() + ' value attribute references variables with types in multiple doamains.'):
	value

endpackage